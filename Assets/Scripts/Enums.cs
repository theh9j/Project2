using UnityEngine;

public enum ColorType {
    Red,
    Green,
    Blue,

    Yellow,
    Orange,
    Brown,
    Emerald,
    LightBlue,

    Pink,
    Magenta,
    Purple,

    White,
    Beige,
    Gray,
    Black,

    Invalid
}

public enum InputType {
    Computer,
    Mobile
}

public enum WaitingSlotState {
    Open,
    Occupied,
    Locked
}
